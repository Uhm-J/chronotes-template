# Development Environment Configuration
# Copy this file to .env for local development

# Server Configuration
PORT=8080
ENVIRONMENT=development
FRONTEND_PATH=frontend/dist

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=chronotes_dev
DB_SSLMODE=disable

# Google OAuth Configuration
# Get these from https://console.cloud.google.com/
# 1. Go to Google Cloud Console
# 2. Create a new project or select existing
# 3. Enable Google+ API
# 4. Go to Credentials > Create OAuth 2.0 Client ID
# 5. Set redirect URI to: http://localhost:8080/v1/auth/google/callback
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here
GOOGLE_REDIRECT_URL=http://localhost:8080/v1/auth/google/callback

# Setup Instructions:
# 1. Copy this file to .env: cp env.development.template .env
# 2. Replace 'your_password_here' with your PostgreSQL password
# 3. Replace Google OAuth credentials with your actual values
# 4. Make sure PostgreSQL is running: brew services start postgresql (macOS)
# 5. Create the database: createdb chronotes_dev
# 6. Run the server: go run cmd/server/main.go 